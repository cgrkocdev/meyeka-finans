"use client";
import {useState} from "react";import {ArrowRight,LockKeyhole,UserRound} from "lucide-react";import {useAuth} from "@/components/auth-provider";
export default function Login(){const {login}=useAuth();const [error,setError]=useState("");function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);if(!login(String(f.get("username")),String(f.get("password"))))setError("Kullanıcı adı veya şifre hatalı.")}return <main className="grid min-h-screen place-items-center bg-[#f7f7f5] p-5"><div className="w-full max-w-[420px]"><div className="mb-8 flex justify-center gap-3"><div className="grid size-11 place-items-center rounded-full bg-black text-xl font-bold text-[#f5c400]">M</div><div className="leading-5"><b className="text-xl">meyeka</b><div className="font-bold text-[#d6aa00]">finans</div></div></div><form onSubmit={submit} className="card p-7"><h1 className="text-center text-2xl font-bold">Tekrar hoş geldiniz</h1><p className="mt-2 text-center text-sm text-[#777]">Finans panelinize giriş yapın.</p><label className="mt-7 block text-sm font-semibold">Kullanıcı adı</label><div className="mt-2 flex h-12 items-center gap-3 rounded-xl border px-3"><UserRound size={18} className="text-[#999]"/><input name="username" required autoComplete="username" className="w-full outline-none" placeholder="Kullanıcı adınızı girin"/></div><label className="mt-4 block text-sm font-semibold">Şifre</label><div className="mt-2 flex h-12 items-center gap-3 rounded-xl border px-3"><LockKeyhole size={18} className="text-[#999]"/><input name="password" type="password" minLength={4} required autoComplete="current-password" className="w-full outline-none" placeholder="Şifrenizi girin"/></div>{error&&<p className="mt-3 text-xs text-red-600">{error}</p>}<button className="mt-6 flex h-12 w-full items-center justify-center gap-2 rounded-xl bg-black font-semibold text-white">Giriş yap<ArrowRight size={17}/></button></form></div></main>}
