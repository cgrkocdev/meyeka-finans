"use client";
import {useState} from "react";import {KeyRound,LogOut,ShieldCheck} from "lucide-react";import {useAuth} from "./auth-provider";
export function SecuritySettings(){const {changePassword,logout}=useAuth();const [message,setMessage]=useState<{ok:boolean;text:string}|null>(null);function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const form=new FormData(e.currentTarget);const next=String(form.get("next"));if(next!==String(form.get("confirm"))){setMessage({ok:false,text:"Yeni şifreler eşleşmiyor."});return}const result=changePassword(String(form.get("current")),next);setMessage({ok:result.ok,text:result.message});if(result.ok)e.currentTarget.reset()}return <div className="grid gap-5 md:grid-cols-[1.3fr_.7fr]"><form onSubmit={submit} className="rounded-xl border p-5"><div className="mb-5 flex items-center gap-3"><div className="grid size-10 place-items-center rounded-xl bg-[#fff3b4]"><KeyRound size={19}/></div><div><b>Şifreyi değiştir</b><p className="text-xs text-[#888]">Hesabınız için yeni bir giriş şifresi belirleyin.</p></div></div>{[["Mevcut şifre","current"],["Yeni şifre","next"],["Yeni şifre tekrar","confirm"]].map(([label,name])=><label className="mb-3 block text-xs font-semibold" key={name}>{label}<input name={name} type="password" required minLength={4} className="mt-1.5 h-11 w-full rounded-xl border px-3 text-sm font-normal outline-none focus:border-[#c9a500]"/></label>)}{message&&<p className={`mb-3 text-xs ${message.ok?"text-[#168953]":"text-red-600"}`}>{message.text}</p>}<button className="h-10 rounded-xl bg-black px-4 text-sm font-semibold text-white">Şifreyi güncelle</button></form><div className="space-y-4"><div className="rounded-xl border p-5"><ShieldCheck className="mb-3"/><b className="text-sm">Giriş hesabı</b><p className="mt-2 text-xs text-[#888]">Kullanıcı adı: <strong className="text-black">meyeka</strong></p></div><button onClick={logout} className="flex w-full items-center justify-center gap-2 rounded-xl border border-red-200 p-3 text-sm font-semibold text-red-600 hover:bg-red-50"><LogOut size={17}/> Oturumu kapat</button></div></div>}
